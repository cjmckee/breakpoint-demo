import type { PlayerProfile as IPlayerProfile, PlayerStats, PlayStyle, CourtSurface, ShotType, StatName, StatCategory } from '../types/index.js';
export declare class PlayerProfile implements IPlayerProfile {
    readonly id: string;
    readonly name: string;
    stats: PlayerStats;
    energy: number;
    form: number;
    confidence: number;
    level: number;
    experience: number;
    matchesPlayed: number;
    matchesWon: number;
    constructor(id: string, name: string, stats?: Partial<PlayerStats>);
    private createDefaultStats;
    getStatForShot(shotType: ShotType): number;
    getStat(statName: StatName): number;
    updateStat(statName: StatName, value: number): void;
    applyStatBoosts(boosts: Partial<Record<StatName, number>>): void;
    get overallRating(): number;
    getStatCategoryAverage(category: StatCategory): number;
    getCurrentForm(): number;
    get playStyle(): PlayStyle;
    get preferredSurface(): CourtSurface;
    reduceEnergy(amount: number): void;
    restoreEnergy(amount: number): void;
    updateConfidence(matchResult: 'win' | 'loss', competitiveness: number): void;
    gainExperience(amount: number): void;
    clone(): PlayerProfile;
    toJSON(): Record<string, any>;
    static fromJSON(data: Record<string, any>): PlayerProfile;
    static createOpponent(name: string, targetRating: number): PlayerProfile;
}
